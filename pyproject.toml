[project]
name = "campus-affairs-assistant"
version = "0.1.0"
description = "RAG and LangGraph based campus affairs assistant."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

